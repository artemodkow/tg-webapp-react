/*devices apple no support hooks(only for android and desktop)*/
/*
const tg = window.Telegram?.WebApp;

export  function usetelegram() {

    return{
        tg,
        username: tg.initDataUnsafe?.user?.username,
    }
}
*/
